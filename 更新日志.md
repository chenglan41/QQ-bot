# 2026/7/23
## 1.4.2
+ 新增 DE,checkNumber,clearContent,moveContent 等工具
+ 修复表情发送需要手动填写绝对路径的的Bug
+ 修复拼写错误(var.json: skill -> prompt)
## 1.8.3
+ 新增 .check .token .menu 等菜单工具
+ 将黑名单移至 var.json
+ 新增管理员列表
# 2026/7/24
## 1.11.7
+ 将封存 聊天记录,清除聊天记录,统计封存的聊天记录 等功能移至菜单
+ 删除了 msg自动去除空格 的功能
+ 新增获取当前时间的函数
+ 新增 ban,unban 等菜单
+ 新增 exec 功能
# 2026/7/25
## 1.19.10
+ 删除 dync 动态变量，

用自动保存的变量space以防止多次读取文件
+ 删除 .seal .clear .count 等菜单
+ 新增 .save .clearExec 等菜单
+ 将附属于exec功能下的cmd改名为 cmdRequest
+ 新增 Token 统计功能
+ 修改变量名 queue,TemporaryContent 用更准确的 prompt,content 代替
+ 新增上下文自动转记忆功能
## 1.20.14
+ 优化 .token
+ 优化 prompt 结构，现在

上下文转记忆功能,身份提示词 分别储存在 prompt 下的 memory.md和system.md 中
+ 过滤器补丁:当信息只有一个空格时返回false，防止机器人对着空信息哈气
+ 新增 .clearContent 菜单
+ 补充 .clearExec 的提示
# 2026/7/26
## 1.20.16
+ 新增 tool call 返回提示词
+ 补充 README.md 里关于 tool call 的描述
+ 修复了上下文转记忆使用的 prompt 文档路径引用错误的 BUG
## 1.22.17
+ 新增定时任务
+ 删除 ._DE.bat
+ 新增token统计数据 space.TokenStatistics
## 1.24.17
+ 新增禁言自动闭嘴的功能
+ config 可配置是否自动将上下文转记忆
# 2026/7/28
## 1.25.18
+ 新增Token统计功能，可配置是否开启
+ 将保底发送信息的统计sendMust分开（每个群单独一个sendMust变量，不同于更新前共用一个变量）
# 2026/7/31
## 1.28.20
+ 新增热切换模型功能(.model 菜单)
+ 系统提示词改为固定输入，不会出现遗忘的现象
+ 微调过滤器
+ 新增发送图片，视频功能
+ 微调http/https网页调用截断(考虑到某些模型只有10k上下文)
## 1.28.22
+ 修复某些模型发送 null 消息的 Bug
+ 更改 prompt/memory.md 中的设定(删掉了有关"主人"的称谓)
# 2026/8/12
## 1.30.26
+ 将回复过的信息以文本形式储存，该改动减少parse解析带来的卡顿
+ 更换新的Model储存方式
+ 添加单独容器
+ 将prompt分离出来
+ 删除部分menu功能
+ 修改配置项
## 1.30.27
+ 不单独设立视觉查看工具

+ 修复往持久化变量中推入token统计的Bug（前面的版本已将持久化变量的token统计删掉）
+ 更改群消息的展示方式
+ 修复下载文件炸开的Bug
+ 修复上下文没有预装的Bug
# 2026/8/24
## 1.31.28
+ 新增 AI Agent 沙盒工具集（node-pty 虚拟终端 + 沙盒文件系统，共 14 个工具）
  终端: createProcess / removeProcess / inputProcess / outputProcess
  文件: createFile / createFolder / deleteFile / deleteFolder
        getFileInfo / getFolderInfo / listDir / readFileContent / writeFileContent
  辅助: agentHelp
+ 新增沙盒机制: 每个会话独立目录 data/{会话ID}, 拒绝路径穿越/绝对路径,
  危险命令黑名单(关机/格式化/注册表/账户/杀进程/下载等), 配额限制
+ 询问器改为 Agent 多轮循环: AI 有工具调用时自动执行工具并把结果发回继续询问,
  直到 AI 不再调用工具给出最终回答(单条消息最多循环10轮, 可改 MAX_AGENT_ROUNDS)
+ index.js 集成 Agent 模块(globalThis.Agent), tools.js 新增 14 个 Agent 工具
+ 菜单新增 .agent 命令, 新增 AGENT.md 文档
+ 依赖新增 node-pty ^1.1.0
